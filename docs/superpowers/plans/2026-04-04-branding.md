# Branding Implementation Plan

> **For agentic workers:** Read CLAUDE.md before starting. You are a code execution agent. Do not plan, brainstorm, or load design skills. Execute each task exactly as specified, run tests, commit, stop.

**Goal:** Add Wilson branding to the web interface -- favicon on both pages, small favicon image in the header, and an About modal accessible from the footer.

**Critical constraints:**
- Do NOT use sse_starlette
- Do NOT use Unicode in print() calls
- Do NOT commit to main -- all commits go to feature/branding
- No external dependencies -- everything inline or from existing files

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Modify | `api.py` | Add /static route to serve favicon |
| Modify | `templates/index.html` | Favicon link tag, header image, about modal |
| Modify | `templates/upload.html` | Favicon link tag, header image, about modal |

---

## Task 0: Create Feature Branch

- [ ] **Step 1: Create and switch to feature branch**

```powershell
git checkout main
git pull
git checkout -b feature/branding
git branch
```

Expected: `* feature/branding`

---

## Task 1: Serve Favicon via Static Route

**Files:**
- Modify: `api.py`

The favicon file already exists at `installer/wilson_icon.ico`.
Wilson needs a route to serve it so browsers can load it.

- [ ] **Step 1: Add static file serving to api.py**

Find the imports section in `api.py`. Add `FileResponse` to the FastAPI imports:

```python
from fastapi.responses import HTMLResponse, StreamingResponse, FileResponse
```

Find the section after the `/health` endpoint. Add:

```python
@app.get("/favicon.ico", include_in_schema=False)
async def favicon():
    """Serve the Wilson favicon from the installer directory."""
    import pathlib
    ico_path = pathlib.Path(__file__).parent / "installer" / "wilson_icon.ico"
    if ico_path.exists():
        return FileResponse(str(ico_path), media_type="image/x-icon")
    # Fallback: return empty 204 response if icon not found
    from fastapi.responses import Response
    return Response(status_code=204)
```

- [ ] **Step 2: Run tests**

```powershell
.\venv\Scripts\python.exe -m pytest tests/ -v
```

Expected: All tests pass.

- [ ] **Step 3: Commit**

```powershell
git add api.py
git commit -m "feat: serve favicon.ico from installer/wilson_icon.ico"
```

---

## Task 2: Add Favicon and Header Image to index.html

**Files:**
- Modify: `templates/index.html`

- [ ] **Step 1: Add favicon link tag to head**

Find the `<head>` section in `index.html`. Add before the closing `</head>` tag:

```html
<link rel="icon" type="image/x-icon" href="/favicon.ico">
```

- [ ] **Step 2: Add small favicon image to header**

Find the `<header>` element in `index.html`. The header currently contains
an `<h1>Wilson</h1>` element and a tagline. Add a small favicon image
immediately before the `<h1>`:

```html
<img src="/favicon.ico" alt="Wilson" width="24" height="24"
     style="margin-right:0.5rem;vertical-align:middle;opacity:0.9;">
```

- [ ] **Step 3: Add About modal**

Find the footer in `index.html`. Update the existing footer link from plain
text to an anchor that opens the About modal:

Replace any existing "Wilson v0.0.5" or similar text in the footer with:

```html
<span>
    <a href="#" onclick="document.getElementById('about-modal').style.display='flex';return false;"
       style="color:var(--text-muted);text-decoration:none;">
        Wilson v0.1.0
    </a>
    --
    <a href="https://github.com/CYoung83/wilson" target="_blank"
       style="color:var(--text-muted);text-decoration:none;">
        github.com/CYoung83/wilson
    </a>
</span>
```

Add the About modal before the closing `</body>` tag:

```html
<!-- About modal -->
<div id="about-modal"
     onclick="if(event.target===this)this.style.display='none'"
     style="display:none;position:fixed;inset:0;background:rgba(0,0,0,0.7);
            z-index:2000;align-items:center;justify-content:center;">
    <div style="background:var(--bg-card);border:1px solid var(--border);
                border-radius:8px;padding:2rem;max-width:480px;width:90%;
                position:relative;">
        <button onclick="document.getElementById('about-modal').style.display='none'"
                style="position:absolute;top:1rem;right:1rem;background:none;
                       border:none;color:var(--text-muted);font-size:1.2rem;
                       cursor:pointer;">&times;</button>

        <div style="display:flex;align-items:center;gap:0.75rem;margin-bottom:1.25rem;">
            <img src="/favicon.ico" alt="Wilson" width="32" height="32">
            <span style="font-size:1.1rem;font-weight:600;color:var(--text);">Wilson</span>
            <span style="font-size:0.8rem;color:var(--text-muted);">v0.1.0</span>
        </div>

        <p style="color:var(--text);font-size:0.9rem;line-height:1.6;margin-bottom:1rem;">
            Wilson removes the advantage afforded to those who will lie.
        </p>

        <p style="color:var(--text-muted);font-size:0.82rem;line-height:1.6;margin-bottom:1rem;">
            An open-source AI reasoning auditor for legal citations. Wilson verifies
            citations against primary sources and reconstructs the decision chain
            behind AI-generated outputs with enough granularity to assign accountability.
        </p>

        <p style="color:var(--text-muted);font-size:0.82rem;line-height:1.6;margin-bottom:1.25rem;">
            Named for the volleyball in Cast Away -- something real built under duress
            because survival required it.
        </p>

        <div style="display:flex;flex-direction:column;gap:0.4rem;
                    border-top:1px solid var(--border);padding-top:1rem;">
            <a href="https://github.com/CYoung83/wilson" target="_blank"
               style="color:var(--accent);font-size:0.82rem;text-decoration:none;">
                github.com/CYoung83/wilson
            </a>
            <a href="https://www.courtlistener.com" target="_blank"
               style="color:var(--text-muted);font-size:0.82rem;text-decoration:none;">
                Powered by CourtListener -- Free Law Project
            </a>
            <a href="https://www.damiencharlotin.com/hallucinations/" target="_blank"
               style="color:var(--text-muted);font-size:0.82rem;text-decoration:none;">
                Charlotin AI Hallucination Database
            </a>
        </div>

        <div style="margin-top:1rem;padding-top:1rem;border-top:1px solid var(--border);
                    display:flex;justify-content:space-between;align-items:center;">
            <span style="color:var(--text-muted);font-size:0.78rem;">Apache 2.0</span>
            <span style="color:var(--text-muted);font-size:0.78rem;">
                National Standard Consulting LLC -- SDVOSB
            </span>
        </div>
    </div>
</div>
```

- [ ] **Step 4: Run tests**

```powershell
.\venv\Scripts\python.exe -m pytest tests/ -v
```

Expected: All tests pass.

- [ ] **Step 5: Commit**

```powershell
git add templates/index.html
git commit -m "feat: favicon, header image, and About modal in index.html"
```

---

## Task 3: Apply Branding to upload.html

**Files:**
- Modify: `templates/upload.html`

- [ ] **Step 1: Add favicon link tag**

Find the `<head>` section in `upload.html`. Add before closing `</head>`:

```html
<link rel="icon" type="image/x-icon" href="/favicon.ico">
```

- [ ] **Step 2: Add header image**

Find the `<header>` element in `upload.html`. Add the same favicon image
before the `<h1>` or equivalent heading:

```html
<img src="/favicon.ico" alt="Wilson" width="24" height="24"
     style="margin-right:0.5rem;vertical-align:middle;opacity:0.9;">
```

- [ ] **Step 3: Add About modal**

Find the footer in `upload.html`. Add the same "Wilson v0.1.0" link and
About modal from Task 2 Steps 3. Copy the exact same modal HTML.

- [ ] **Step 4: Run tests**

```powershell
.\venv\Scripts\python.exe -m pytest tests/ -v
```

Expected: All tests pass.

- [ ] **Step 5: Commit**

```powershell
git add templates/upload.html
git commit -m "feat: favicon, header image, and About modal in upload.html"
```

---

## Task 4: Update Version String

**Files:**
- Modify: `api.py`

The health endpoint and FastAPI title currently show version 0.0.5 or 0.1.0
inconsistently. Standardize to 0.1.0.

- [ ] **Step 1: Update version in api.py**

Find `version="0.0.5"` or `version="0.1.0"` in the `FastAPI(...)` instantiation
and the `/health` endpoint. Set both to `"0.1.0"`.

Also update any hardcoded version strings in the footer of index.html and
upload.html from `v0.0.5` to `v0.1.0` if they exist.

- [ ] **Step 2: Run tests**

```powershell
.\venv\Scripts\python.exe -m pytest tests/ -v
```

- [ ] **Step 3: Commit**

```powershell
git add api.py templates/index.html templates/upload.html
git commit -m "chore: standardize version string to v0.1.0 across api.py and templates"
```

---

## Task 5: Smoke Test and Push

- [ ] **Step 1: Run full test suite**

```powershell
.\venv\Scripts\python.exe -m pytest tests/ -v
```

- [ ] **Step 2: Start server and verify manually**

```powershell
.\venv\Scripts\python.exe -m uvicorn api:app --host 0.0.0.0 --port 8000
```

Verify in browser:
- Browser tab shows Wilson favicon
- Header shows small favicon image next to Wilson title
- Footer "Wilson v0.1.0" link opens About modal
- Modal shows mission statement, links, license, attribution
- Same on /upload page

- [ ] **Step 3: Verify branch**

```powershell
git branch
```

Expected: `* feature/branding`

- [ ] **Step 4: Push**

```powershell
git push -u origin feature/branding
```

- [ ] **Step 5: Report**

Report test results, commit hashes, push confirmation, and any visual issues
noted during manual verification.

---

## Completion Checklist

- [ ] Branch `feature/branding` created
- [ ] `/favicon.ico` route in `api.py`
- [ ] Favicon `<link>` tag in both HTML pages
- [ ] Small favicon image in header of both pages
- [ ] About modal in both pages -- mission, links, license, attribution
- [ ] Version string standardized to v0.1.0
- [ ] All tests pass
- [ ] Feature branch pushed to GitHub
